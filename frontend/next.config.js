/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Empty turbopack config to silence the "webpack vs turbopack" error
  turbopack: {},
};

export default nextConfig;
